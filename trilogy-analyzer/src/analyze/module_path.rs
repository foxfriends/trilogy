use crate::Analyzer;
use trilogy_lexical_ir::Evaluation;
use trilogy_parser::syntax::ModulePath;

pub(super) fn analyze_module_path(
    _analyzer: &mut Analyzer,
    _module_path: ModulePath,
) -> Evaluation {
    todo!()
}
