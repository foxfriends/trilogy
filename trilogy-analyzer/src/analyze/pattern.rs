use crate::Analyzer;
use trilogy_lexical_ir::Evaluation;
use trilogy_parser::syntax::Pattern;

pub(super) fn analyze_pattern(_analyzer: &mut Analyzer, _pattern: Pattern) -> Evaluation {
    todo!()
}
