use crate::Analyzer;
use trilogy_lexical_ir::Evaluation;
use trilogy_parser::syntax::Expression;

pub(super) fn analyze_poetry(_analyzer: &mut Analyzer, _expression: Expression) -> Evaluation {
    todo!()
}
