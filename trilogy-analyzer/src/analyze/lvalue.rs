use crate::Analyzer;
use trilogy_lexical_ir::LValue;
use trilogy_parser::syntax::Expression;

pub(super) fn analyze_lvalue(_analyzer: &mut Analyzer, _expression: Expression) -> LValue {
    todo!()
}
