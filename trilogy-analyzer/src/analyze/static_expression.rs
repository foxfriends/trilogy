use crate::Analyzer;
use trilogy_lexical_ir::Evaluation;
use trilogy_parser::syntax::Expression;

pub(super) fn analyze_static(_analyzer: &mut Analyzer, expression: Expression) -> Evaluation {
    todo!()
}
