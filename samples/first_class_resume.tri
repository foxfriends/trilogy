func divBy x 0 = x / yield 'INF
func divBy x y = x / y

func doubleInto x into = into (2 * x)

proc main!() {
    exit with divBy 8 0
        when 'INF cancel doubleInto 2 (resume)
        else yield
}
