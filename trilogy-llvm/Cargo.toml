[package]
name = "trilogy-llvm"
version = "0.1.0"
edition = "2021"

[dependencies]
inkwell = { version = "0.5.0", features = ["llvm18-0"] }
num = "0.4.3"
trilogy-ir = { path = "../trilogy-ir/" }
trilogy-parser = { path = "../trilogy-parser/" }

[lints]
workspace = true
