test "passes when the expression evaluates to true" {
    assert is is true
}

test "fails when the expression evaluates to false" {
    assert !(is is false)
}

test not "it is a runtime type error if the expression is not a boolean" {
    for is unit {}
}
