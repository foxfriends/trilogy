module core at "trilogy:core"

export dbg
proc dbg!(value) {
  core::print!($"${value}\n")
  return value
}

export trace
proc trace!(label, value) {
  core::print!($"${label}: ${value}\n")
  return value
}
