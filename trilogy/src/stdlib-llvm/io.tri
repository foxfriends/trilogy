module libc at "trilogy:c"

export print
proc print!(value) {
  let err = libc::printf(value_string);
  if err < 0 { yield 'ioerror }
}

export print
proc println!(value) {
  let err = libc::printf(value_string);
  if err < 0 { yield 'ioerror }
}
