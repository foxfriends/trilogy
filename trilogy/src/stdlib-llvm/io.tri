module libc at "trilogy:c"

export print
proc print!(value) {
  let err = libc::printf!(value)
  if err < 0 {
    yield 'ioerror
  }
}

export println
proc println!(value) {
  let err = libc::printf!(value)
  if err < 0 {
    yield 'ioerror
  }
}
